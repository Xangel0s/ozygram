# Ozy Brain

Local Python reasoning worker used by the Rust MCP server.

It reads JSON from stdin and writes JSON to stdout. It does not modify files, run shell commands, or write to the database directly.
